RavenDB is accessed through a NuGet package:

So it is included in the packages:

And its path will be of the form:

Paramore\Rewind\Packages\RavenDB.Server.X.X.XXXX\tools

For example the path at time of writing is:

C:\Users\ian\OSS\Paramore\Rewind\Packages\RavenDB.Server.2.0.2230\tools

Run the Raven Server from here. My usual approach is to navigate to the directory in explorer, and type cmd in the address bar, launching a command window that you can run the execuable in

