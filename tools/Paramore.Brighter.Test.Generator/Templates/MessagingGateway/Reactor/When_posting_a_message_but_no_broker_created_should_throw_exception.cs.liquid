// <auto-generated>
// This file is auto-generated by Paramore.Brighter.Test.Generator
// </auto-generated>
using System;
using System.Collections.Generic;
using System.Threading;

using Xunit;

namespace {{ Namespace }}.MessagingGateway{{ Prefix }}.Reactor;
{% if Category != null %}
[Trait("Category", "{{Category}}")]{% endif %}
public class WhenPostingAMessageButNoBrokerCreatedShouldThrowException : IDisposable
{
    private readonly IAmAMessageGatewayReactorProvider _messageGatewayProvider;
    private readonly IAmAMessageBuilder _messageBuilder;

    private List<Message> _sentMessages = [];

    private {{ Publication }}? _publication;

    private IAmAMessageProducerSync? _producer;

    public WhenPostingAMessageButNoBrokerCreatedShouldThrowException()
    {
        _messageGatewayProvider = new {{ MessageGatewayProvider }}();
        _messageBuilder = new {{ MessageBuilder }}();
    }

    public void Dispose()
    {
        try
        {
            _messageGatewayProvider.CleanUp(_producer, null, _sentMessages);
        }
        catch
        {
            // The infrastructure might not exist, so we ignore any cleanup errors
        }
    }

    [Fact]
    public void When_posting_a_message_but_no_broker_created_should_throw_exception()
    {
        try
        {
            // Arrange
            _publication = _messageGatewayProvider.CreatePublication(_messageGatewayProvider.GetOrCreateRoutingKey());
            _publication.MakeChannels = OnMissingChannel.Validate;
            _producer = _messageGatewayProvider.CreateProducer(_publication);

            var message = _messageBuilder.SetTopic(_publication.Topic!).SetPartitionKey(PartitionKey.Empty).Build();
            _sentMessages.Add(message);

            // Act
            _producer.Send(message);

            // Assert
            Assert.Fail("We are expected to throw an exception");
        }
        catch (Exception ex) when (ex is not Xunit.Sdk.XunitException)
        {
            Assert.True(true);
        }
    }
}
